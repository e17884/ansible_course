---
- name: Installer apache2 et configure la page d'acceuil
  hosts: all

  tasks:
    - name: Install apache2
      ansible.builtin.apt:
        update_cache: true
        name: apache2
        state: present
